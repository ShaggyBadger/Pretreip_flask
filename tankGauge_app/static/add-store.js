// hey remember to put tgmhs csrf token in base header so i can grab it in here
async function generate_chart(tank_id) {
  console.log("Heyyyyy this is firing off");
  try {
    // do stuff here
    };
  } catch (error) {
    console.error("Error generating store stuff:", error);
  }
}
